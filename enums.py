'''
This python file defines an enum class Zone, which will be used to define the zone of each location

'''

# Importing the required classes
from enum import Enum

class Zone(Enum):
    CELESTE = 1
    VERDE = 2
    TIENDAS = 3